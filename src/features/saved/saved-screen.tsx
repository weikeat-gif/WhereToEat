import { ScreenPlaceholder } from '@/components/screen-placeholder';

export function SavedScreen() {
  return (
    <ScreenPlaceholder
      title="Saved"
      description="Sign in to sync favourite restaurants across devices."
    />
  );
}
