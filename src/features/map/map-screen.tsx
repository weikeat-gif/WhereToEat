import { ScreenPlaceholder } from '@/components/screen-placeholder';

export function MapScreen() {
  return (
    <ScreenPlaceholder
      title="Map"
      description="Restaurant pins and the synchronized result sheet are being built here."
    />
  );
}
