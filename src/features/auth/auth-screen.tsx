import { ScreenPlaceholder } from '@/components/screen-placeholder';

export function AuthScreen() {
  return (
    <ScreenPlaceholder
      title="Sign in"
      description="Google, Apple and email authentication will be available here."
    />
  );
}
